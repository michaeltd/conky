#!/usr/bin/env bash

main()
{

    isValidLink(){:}

    isValidTheme(){:}

    themeHasFonts(){:}

    themeHasRc(){:}

    themeHasConkyrc(){:}
    
}

# Run on execute not source!
[[ "${BASH_SOURCE[0]}" == "${0}"]] && main "${@}"
